@model StatusResult
@{
    ViewBag.Title = "Error";
}
<div class="my-5">
    <h1 class="display-4 text-danger">The ressource cannot be found !</h1>
    <p class="lead">
        @Model.Message
    </p>
    <hr class="my-4" />
    <p  >
        Path: @Model.Path <br />
        @if (!string.IsNullOrEmpty(Model.QS))
        {
                
    <span>
        Query Strings: @Model.QS
    </span>
            
        }
    </p>
    <a asp-controller="Employee" asp-action="index" class="btn btn-outline-info">Back to Home</a>
</div>